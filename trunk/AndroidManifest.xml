<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.stericson.permissions"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".Permissions"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    <activity android:launchMode="singleInstance" android:name="ApplicationSpecifics"></activity>
<activity android:launchMode="singleInstance" android:name="MasterPermissions"></activity>

<activity android:name="SharedPermissions" android:launchMode="singleInstance"></activity>
<activity android:name=".SharedPermissionsSpecific" android:launchMode="singleInstance"></activity>
</application>
    <uses-sdk android:minSdkVersion="1" />

</manifest> 